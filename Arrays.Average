public class Arrays {

    public static void main(String[] agrs){

        int[] numbers = {79,11,4,7,30};
        int sum = 0;
        for(int i = 0;i< numbers.length;i++)
        sum = sum + numbers[i];
        int average = sum/ numbers.length;
        System.out.println("Avarage of array:" + average);

    }
}
